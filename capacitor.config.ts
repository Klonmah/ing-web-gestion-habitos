import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ing-web-gestion-habitos',
  webDir: 'dist'
};

export default config;
